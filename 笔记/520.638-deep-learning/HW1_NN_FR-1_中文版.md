#  ECE 520.438 & 520.638: 深度学习

## 作业 1

### 2026 年春季

**评分标准：** 评分将基于你开发的代码以及总结研究发现的作业报告。代码可以使用任何你喜欢的编程语言，并且允许使用库函数。如果可能，请使用 LaTeX 撰写报告。

---

## 1 第一部分（20 分）

回答以下问题。

1. 什么是手工设计的特征（hand-crafted features）？请至少给出三个手工设计特征的例子。

2. 什么是学习得到的特征（learned features）？请至少给出三个学习特征的例子。

3. 简要讨论手工设计特征与学习特征相比的一些优缺点。

4. PCA（主成分分析）有哪两种表述方式？

5. LDA（线性判别分析）将维度从原始特征数量降低到多少维？

6. LDA 有哪些局限性？

7. 给出 PCA 的两个缺点。

8. 计算机视觉中的许多特征以直方图的形式表示。给定两个直方图，有哪些距离度量可以用于比较它们？请至少给出三个例子。

9. 为什么 $\ell_0$ 范数能够刻画稀疏性？

10. 为什么我们用 $\ell_1$ 范数来近似 $\ell_0$ 范数？

11. k-means 聚类有哪些缺点？

12. 最近邻算法（Nearest Neighbor）和 k-近邻算法（k-Nearest Neighbor）之间有什么区别？

13. 简要描述视觉词袋（Visual Bag of Words）特征是如何提取的。

14. 简要描述交叉验证（Cross-Validation）。

15. 稀疏编码（Sparse Coding）和字典学习（Dictionary Learning）之间有什么区别？

---

## 2 第二部分：人脸识别：k-NN（40 分）

在本练习中，你将实现并评估我们在课堂上学习的 k-近邻（k-Nearest Neighbor）算法。

### 2.1 Extended YaleB 数据集

1. Extended YaleB 数据集中的原始图像已被裁剪并调整为 32×32 大小。该数据集包含 38 个个体，每个个体大约有 64 张不同光照条件下的近正面人脸图像。该数据集的样本图像如图 1 所示。从课程资料库下载文件 `YaleB-32x32.mat`。该文件包含变量 `'fea'` 和 `'gnd'`。`'fea'` 的每一行是一张人脸图像，`'gnd'` 是对应的标签。

   随机选取每个个体的 (m = 10, 20, 30, 40, 50) 张带标签的图像组成训练集，将数据集中的剩余图像作为测试集。对这五种划分分别应用 k-NN 算法（k = 1），并记录相应的分类错误率。使用欧氏距离度量，即 $d(x, y) = \|x - y\|_2$。分类错误率 E 定义如下：

$$E = \frac{\sum_{i=1}^{n} \mathbf{1}[\hat{l}(x_i) \neq l(x_i)]}{n} \times 100$$

   其中 $n$ 是测试样本数量，$\hat{l}(x_i)$ 是第 $i$ 个测试样本的分类结果，$l(x_i)$ 是该样本的真实类别。在同一张图中绘制"分类错误率 vs 训练样本数量"曲线。总结你的发现。

   > 图 1：Extended YaleB 数据集的样本图像。

2. 对 k = 2, 3, 5, 10 重复上述步骤，并绘制错误率 E 关于 k 的变化曲线。错误率是否随 k 的增大而降低？错误率是否应该总是随 k 增大而降低？绘制一些被误分类样本的 k 个最近邻。

3. 设 k = 3，每个个体选取 m = 30 张带标签的图像组成训练集，将剩余图像作为测试集。将距离度量替换为 $\|x - y\|_p$，其中 p = 1, 3, 5, 10，并绘制错误率关于 p 的变化曲线。距离度量是否影响错误率？

4. 不使用像素强度作为特征，而是从图像中提取 LBP（局部二值模式）和 HOG（方向梯度直方图）特征。对 p = 1, 2 重复步骤 3。像素强度、LBP 和 HOG 特征分别对应的错误率是多少？

5. 在本练习中你获得的最低错误率是多少？

### 2.2 验证集

随机抽取每个个体的 20 张图像组成测试集。使用剩余数据组成训练集。进一步将训练集适当划分为新的训练集和验证集。使用验证集来优化 k-NN 算法的参数（即 k 和 p）。本练习请使用像素强度进行实验。

在本练习中你获得的最低错误率是多少？对应的 k 和 p 值分别是多少？

---

## 3 第三部分 - 人脸识别：其他算法（40 分）

在本部分中，你将实现并评估我们在课堂上学习的四种基本人脸识别和图像分类算法。这四种算法分别是：特征脸（Eigenfaces / PCA）、Fisher 脸（Fisherfaces / LDA）、支持向量机（SVM）和基于稀疏表示的分类（SRC），在 Extended YaleB 数据集上进行评估。

你需要按照 2.1.1 中相同的步骤进行实验并总结各算法的发现，即：随机选取每个个体的 (m = 10, 20, 30, 40, 50) 张带标签的图像（来自 YaleB）组成训练集，将数据集中的剩余图像作为测试集。对这五种划分分别应用四种算法，并记录相应的分类错误率。使用欧氏距离度量，即 $d(x, y) = \|x - y\|_2$。分类错误率 E 定义如下：

$$E = \frac{\sum_{i=1}^{n} \mathbf{1}[\hat{l}(x_i) \neq l(x_i)]}{n} \times 100$$

其中 $n$ 是测试样本数量，$\hat{l}(x_i)$ 是第 $i$ 个测试样本的分类结果，$l(x_i)$ 是该样本的真实类别。为每种算法分别绘制"分类错误率 vs 训练样本数量"曲线。总结你的发现。

**注意：** 对于 LDA，最多有 $c - 1$ 个非零广义特征值，因此降维后空间的维度上界为 $c - 1$，其中 $c$ 是个体的数量。
